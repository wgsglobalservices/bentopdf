name = "bentopdf-site"
main = "site-worker.js"
compatibility_date = "2024-01-01"

[assets]
directory = "../dist"

# Deploy command:
# npm run build:cloudflare-worker
# (uses NODE_OPTIONS=--max-old-space-size=3072)
# npm run deploy:cloudflare-worker

# Optional custom domain route:
# routes = [
#   { pattern = "pdf.example.com/*", zone_name = "example.com" }
# ]
